{
  "name": "api",
  "version": "1.0.0",
  "description": "The `example-*` api's in this folder are just examples of Lambda APIs.  Only the `example` Lambda is actually used in the template to demonstrate the full loop.  The APIs should be removed after you start adding your own APIs or choose another server technology.  The custom-build and custom-container-build should only be used if LambdaPython and NodePython constructs won't work for your use case.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@types/ws": "^8.5.4",
    "@typescript-eslint/eslint-plugin": "5.22.0",
    "@typescript-eslint/parser": "5.22.0",
    "aws-sdk": "^2.1320.0",
    "constructs": "^10.0.0",
    "eslint": "^8.9.0",
    "eslint-config-prettier": "8.5.0",
    "eslint-plugin-prettier": "4.0.0",
    "source-map-support": "^0.5.16",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.276.0",
    "@aws-sdk/client-forecast": "^3.276.0",
    "@aws-sdk/client-glue": "^3.276.0",
    "@aws-sdk/client-s3": "^3.278.0",
    "@aws-sdk/credential-providers": "^3.276.0",
    "@aws-sdk/s3-request-presigner": "^3.278.0",
    "csv-parser": "^3.0.0",
    "ws": "^8.12.1"
  }
}
